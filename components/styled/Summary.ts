"use client";

import styled from "styled-components";

export const Summary = styled.div`
  position: absolute;
  top: 0;
  left: 0%;
  transform: translateX(10px) translateY(-50%);
  background: var(--page-bg, #1b1f22);
  padding: 0 10px;
  border: 1px solid var(--color-faded-white);
  border-top: 0;
  border-bottom: 0;
  line-height: 1;
`;
